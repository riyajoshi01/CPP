#include <iostream>
using namespace std;

int main(){
    int a,num=153,rem,arm=0;
    cout<<"enter a number: ";
    cin>>a;
    while(a>0){
        rem=a%10;
        arm=arm+rem*rem*rem;
        a=a/10;
        if(num==arm){
            cout<<"ARMSTRONG NUMBER";
        }
    }
    cout<<endl;
}
